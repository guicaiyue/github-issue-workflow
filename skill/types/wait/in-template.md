# `wait` 输入模板

## 进入该阶段前应具备的信息

最小输入信息应尽量覆盖：

- 正在等待什么
- 为什么必须等待
- 什么条件成立后才能恢复
- 恢复后建议先回到哪个阶段

建议字段：

- `Current type: wait`
- `Waiting for`
- `Why waiting is necessary`
- `Resume condition`
- `Suggested next type`
